import { ReactNode } from 'react';

export const metadata = {
  title: "Apply - AriesView Careers",
  description: "Apply for a position at AriesView and help transform real estate analytics",
};

export default function ApplyLayout({ children }: { children: ReactNode }) {
  return children;
} 